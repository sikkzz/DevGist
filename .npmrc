# DevGist pnpm 설정

# peer dependency 미충족 시 경고만 띄우고 install 계속.
# Next 16 / React 19 생태계에 아직 peer 범위 안 맞춘 패키지가 있어 필요.
strict-peer-dependencies=false

# peer dependency 자동 설치 (pnpm 9 기본값이지만 명시).
auto-install-peers=true
