# Neon Postgres 연결 문자열 (pooled connection 권장 — serverless 환경에서 커넥션 고갈 방지)
# Neon 대시보드 → Connection Details → "Pooled connection" 복사
DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler.REGION.aws.neon.tech/DB?sslmode=require"
